<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" />
    <corners android:radius="50dp" /><!-- Background color -->
    <stroke android:color="@color/text_color" android:width="2dp" /> <!-- Border color and width -->
</shape>
